/**
 * Canonical version stamps for persisted HR snapshots and restore compatibility checks.
 * Bump when serialization shape or restore semantics change (engineVersion), or when
 * workforce/OH formula definitions change incompatibly (formulaVersion).
 */
export const HR_WORKFORCE_ENGINE_VERSION = 1;
export const HR_WORKFORCE_FORMULA_VERSION = 1;
